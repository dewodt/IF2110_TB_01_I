#include "../../masukan/masukan.c"
#include "../../masukan/masukanint.c"
#include "../matrixteman/matrixteman.c"
#include <stdio.h>

int main()
{
    MatrixTeman mt;
    MASUKANint inputint;
    bacaInt(inputint);

    switch (inputint)
    {
    case 1:
        // Create Matrix teman, Input Matrix Teman dan Display
        createMatrixTeman(20, 20, &mt);
        for (int i = 0; i <)
        case 2:

            creat default : break;
    }
}